# create Api keys from livekit login livekit and create Apikey put those in env file 
LIVEKIT_URL,
LIVEKIT_API_KEY,
LIVEKIT_API_SECRET

Install UV package and run 
uv run: Run a script.
uv add --script: Add a dependency to a script.
uv remove --script: Remove a dependency from a script.